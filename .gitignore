logs/
*.egg-info/
*__pycache__*
data/
*.venv/
.vscode
.env
conf.yaml
/tmp
# eval 运行产物（报告 / define_tool 隔离目录 / 任务输出）
eval/report/
eval/.agent-tools/
eval/data/out/

# eval 数据 fixture 例外（data/ 全局忽略，但这些必须入库）
!eval/data/
!eval/data/*.csv
!eval/data/*.log
!eval/data/*.txt
datasets/
# Command Code 本地配置（会话 / taste / 缓存，不入库）
.commandcode/

# MCP server 配置（可含明文 API key / token，一律不入库；参照 .agent/mcp.yaml.example）
.agent/mcp.yaml
